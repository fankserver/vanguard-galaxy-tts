# Build
bin/
obj/
*.user
*.suo
.vs/

# Plugin's compile-time references
# - Assembly-CSharp.dll: publicized STUB of the game's assembly (method
#   signatures only, no IL bodies). Committed so CI can build. Regenerate
#   after game updates via:
#     assembly-publicizer --strip <game>/Assembly-CSharp.dll \
#                         -o VGTTS/lib/Assembly-CSharp.dll
#   ⚠️  --strip is NOT optional: without it, the stub contains the full game
#   IL (method bodies) and committing it constitutes a copyright leak.
# - Newtonsoft.Json.dll: symlinked from the game install locally, pulled via
#   NuGet on CI. Not committed.
VGTTS/lib/Newtonsoft.Json.dll
# - VGModAPI.Abstractions.dll: symlink to the sibling vanguard-galaxy-api
#   Release build, refreshed by `make link-api`. Never committed.
VGTTS/lib/VGModAPI.Abstractions.dll
VGTTS/lib/*.pdb
!VGTTS/lib/.gitkeep
!VGTTS/lib/Assembly-CSharp.dll

# Legacy runtime TTS caches (plugin uses BepInEx/cache/VGTTS at runtime)
cache/
*.wav
*.mp3

# Pre-render artifacts
# Track: prerender/<speaker>/*.ogg and manifest.json (small pack, shipped in repo)
# Skip: shootout/review intermediates and debug WAV variants
prerender/shootout/
prerender/review_report.*
prerender/spot_check.md
prerender/variants/

# OS
.DS_Store
Thumbs.db

# Bundled TTS tools — large binaries, re-downloadable via `make download-*`
# List specific subdirs (can't un-ignore under a wholly-excluded parent in git)
tools/piper/
tools/sherpa/
tools/kokoro/
tools/voices/

# Within the pipeline dir, exclude only data/cache subdirs
tools/prerender/variants/
tools/prerender/accent_probes/
tools/prerender/echo_tips.json
# Build-time intermediates — regenerated by resolve_speakers.py
tools/prerender/render_plan.json
tools/prerender/resolved_lines.json
**/__pycache__/

# Track reference WAVs (needed for reproducible renders across machines)
# Must explicitly un-ignore both dir + contents since *.wav matches files
!tools/prerender/references/
!tools/prerender/references/*.wav
.claude/
dist/
